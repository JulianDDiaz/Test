on: [push]

jobs:
  run_script:
    runs-on: ubuntu-latest
    name: create_and_save
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Execute
        run: node test.js